<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- background -->
    <solid android:color="@color/Yellow_Medium" />

    <!-- radius -->
    <corners android:radius="24dp" />

    <!-- borders -->
    <stroke
        android:width="2dp"
        android:color="@color/white" />

    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>
